apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress   # 👈 match base ingress name
  namespace: three-tier  # 👈 match base namespace
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: frontend.three-tier.local   # 👈 corrected hostname
      http:
        paths:
          - path: /(.*)
            pathType: Prefix
            backend:
              service:
                name: frontend-service   # 👈 match base service name
                port:
                  number: 80

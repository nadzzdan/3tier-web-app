apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: three-tier
  annotations:
    # Example: add TLS or other controller-specific annotations here if needed
    # nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: 3tierwebapp.local.com
